version: 2

models:
  - name: fct_orders
    description: "Order-level fact"
    columns:
      - name: order_sk
        tests:
          - not_null
          - unique

      - name: order_id
        tests:
          - not_null
          - unique
