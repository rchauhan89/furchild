version: 2

models:
  - name: fct_orders
    description: "Order-level facts aggregated from fct_order_items; minimal tests to unblock CI."
    columns:
      - name: order_sk
        tests:
          - not_null
          - unique
      - name: order_id
        tests:
          - not_null
